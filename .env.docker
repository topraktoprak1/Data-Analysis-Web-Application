# Use this when running with Docker on another machine
FLASK_ENV=production
FLASK_DEBUG=0

# Database - use 'db' as hostname in Docker network
DATABASE_URL=postgresql://postgres:85758.As@db:5432/veri_analizi

# Frontend API - use service name in Docker network
VITE_API_URL=http://web:5000
